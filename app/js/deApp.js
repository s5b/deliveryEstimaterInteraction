'use strict';


/* Declare app level module which depends on services, and controllers for managing the capture of input. */
angular.module('deliveryEstimator', ['deInput.controllers', 'deOutput.controllers']);